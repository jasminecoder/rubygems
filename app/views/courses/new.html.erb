<div class="row">
    <div class="col-md-12">
        <div class="card mb-3">
            <div class="card-header">
                <h1>New Course</h1>
            </div>
            <div class="card-body">
                <%= render 'form', course: @course %>
            </div>
        </div>
        <%= link_to 'Back', courses_path %>
    </div>
</div>