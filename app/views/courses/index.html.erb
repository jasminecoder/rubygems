<div class="container">
    <div class="row">
        <div class="col-md-12">
            <p id="notice"><%= notice %></p>

            <p class="lead">Courses <span class="badge badge-info"><%= @courses.count %> </span></p>
            <div class="row">
                <div class="col-lg-3">
                    <%= search_form_for @ransack_courses, as: :courses_search, url: courses_path do |f| %>
                    <%= f.label :title %>
                    <%= f.search_field :title_cont, placeholder: 'Title', class: 'form-control form-control-sm' %>
                    <%= f.label :description %>
                    <%= f.search_field :short_description_cont, placeholder: 'Description', class: 'form-control form-control-sm' %>
                    <%= f.label :language %>
                    <%= f.select :language_cont, Course.languages , include_blank: true, placeholder: 'Language', class: 'form-control form-control-sm' %>
                    <br>
                    <%= f.label :level %>
                    <%= f.select :level_cont, Course.levels , include_blank: true, placeholder: 'Level', class: 'form-control form-control-sm' %>
                    <br>
                    <%= f.label :price %>
                    <%= f.search_field :price_eq, placeholder: 'Price', class: 'form-control form-control-sm' %>
                    <%= f.label :teacher %>
                    <%= f.search_field :user_email_cont, placeholder: 'Email', class: 'form-control form-control-sm' %>
                    <%= f.submit class:'btn btn-sm btn-success form-control form-control-sm mt-3'%>
                    <br>
                    <%= link_to courses_path, class:'btn btn-sm btn-secondary mt-1' do %>
                    <i class="fas fa-sync"></i>
                    <% end %>
                    <% end %>
                </div>
                <div class="col-lg-9">
                    <%== pagy_bootstrap_nav(@pagy) %>
                    <div class="card-columns">
                        <% @courses.each do |course| %>
                        <%= render 'courses/course', course: course %>
                        <% end %>

                    </div>
                    <%== pagy_bootstrap_nav(@pagy) %>

                </div>

            </div>








        </div>
    </div>

</div>